# Model Weights

Place `best_model.pt` here after training.

**Download trained weights:**
> [Google Drive / HuggingFace link — add after training]

Or train from scratch:
```bash
python train.py --gt_dir /path/to/GT --lr_dir /path/to/NoisyLR
```
